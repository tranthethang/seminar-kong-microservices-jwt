{
  "name": "kong-microservices-jwt",
  "version": "1.0.0",
  "description": "Microservices architecture with Kong API Gateway and JWT authentication",
  "scripts": {
    "test": "mocha integration-test.js --timeout 10000",
    "docker:up": "docker-compose up --build",
    "docker:down": "docker-compose down"
  },
  "keywords": [
    "kong",
    "microservices",
    "jwt",
    "api-gateway"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "mocha": "^10.2.0",
    "chai": "^4.3.7"
  },
  "dependencies": {
    "axios": "^1.6.2"
  }
}
